<%= form_with model: @book do |f| %>
  <h4>title</h4>
  <%= f.text_field :title %>
	
  <h4>body</h4>
  <%= f.text_area :body %>
	
  <%= link_to "Edit", edit_book_path(@book.id) %>
  <%= link_to "Back" %> <!-- 画面トップに戻るには？-->
<% end %>